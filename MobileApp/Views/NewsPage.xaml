<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:MobilApp.ViewModels"
             x:Class="MobilApp.Views.NewsPage"
             x:DataType="viewmodels:NewsViewModel"
             Title="Новости">
    
    <ContentPage.BindingContext>
        <viewmodels:NewsViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="10">
            <toolkit:CarouselView ItemsSource="{Binding News}">
                <toolkit:CarouselView.ItemTemplate>
                    <DataTemplate x:DataType="models:NewsItem">
                        <Frame Padding="0" HasShadow="True" Margin="5">
                            <Grid>
                                <Image Source="{Binding ImageUrl}" Aspect="AspectFill">
                                    <Image.Triggers>
                                        <DataTrigger TargetType="Image" Binding="{Binding ImageUrl}" Value="">
                                            <Setter Property="Source" Value="placeholder.jpg"/>
                                        </DataTrigger>
                                    </Image.Triggers>
                                </Image>
                                <StackLayout Padding="10" BackgroundColor="#80000000">
                                    <Label Text="{Binding Title}" TextColor="White" FontSize="Large"/>
                                    <Label Text="{Binding Description}" TextColor="White" FontSize="Small"/>
                                </StackLayout>
                                <Grid.GestureRecognizers>
                                    <LongPressGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:NewsViewModel}}, Path=ShowReactionsCommand}" CommandParameter="{Binding .}" />
                                </Grid.GestureRecognizers>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </toolkit:CarouselView.ItemTemplate>
            </toolkit:CarouselView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>