<UserControl x:Class="Desktop_app.Views.WorkerEvents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Background="White">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="#FF6B35" Padding="15" CornerRadius="5" Margin="0,0,0,20" Grid.Row="0">
                <TextBlock x:Name="TitleBlock" Text="События сотрудника" 
                           FontSize="18" FontWeight="Bold" Foreground="White"/>
            </Border>

            <Button Content="← Назад" Click="OnBackClicked"  Grid.Row="1"
                    Background="#FF6B35" Foreground="White"
                    Padding="15,10" Width="100" HorizontalAlignment="Left"
                    Margin="0,0,0,20"/>

            <DataGrid x:Name="EventsDataGrid" AutoGenerateColumns="False" Grid.Row="2"
          CanUserAddRows="False" CanUserDeleteRows="False" 
          Background="White" GridLinesVisibility="All"
          BorderBrush="#CCCCCC" BorderThickness="1" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding EventId}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding EventName}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding EventType}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Описание" Width="300" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}"
                       TextWrapping="Wrap"
                       Margin="5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="EmptyMessage" Text="Нет событий для этого сотрудника"  Grid.Row="2"
                       FontSize="14" Foreground="Gray" Margin="0,20,0,0"
                       Visibility="Collapsed" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>